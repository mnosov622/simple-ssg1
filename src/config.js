const config = {
  dev: {
    postsdir: "./content",
    outdir: "./dist",
    lang: "en",
    stylesheet: "https://fonts.googleapis.com/css2?family=Open+Sans&display=swap"
  }
};

module.exports = config;
